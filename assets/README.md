# Media assets

Place your attached animation files here to make the top banner play on GitHub Pages:

- `header.mp4` (preferred for autoplay support)
- optional `header.webm`
- `header.gif` (fallback poster/static fallback)

The page is already configured to autoplay and loop the media at the top.
